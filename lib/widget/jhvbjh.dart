import 'package:flutter/widgets.dart';

class ufyufyy extends StatelessWidget {
  final String t;
  const ufyufyy({Key? key, required this.t}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
